# Tuples

# Tuples is a type of data structure that cannot have its values changed

coordinates = (4, 5)
coordinate = [(4, 5), (1, 3), (8,3)]
print(coordinates)
print(coordinates[0])
print(coordinate[0][1])